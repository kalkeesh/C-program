#include<stdio.h>
int main(){
	 int i,n,sum=0,m;
	 scanf("%d",&n);
	 int arr[n-1];
	 for(i=0;i<n-1;i++){
	     scanf("%d",&arr[i]);
	     sum=sum+arr[i];
	 }
	 m = (n*(n+1))/2 - sum;
	 printf("missing number is = %d",m);
}
 
